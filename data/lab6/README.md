# Lab6 Data Notes

- `results/`: lưu `lab6_part1_outputs.json` và các file quan sát khác từ Transformer pipelines.
- Nếu thêm phần mở rộng (ví dụ fine-tuning), tạo thư mục con mới tại đây.
- Mặc định bỏ qua các file JSON lớn hoặc checkpoint nặng.
